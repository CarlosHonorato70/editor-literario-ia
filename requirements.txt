# Dependências do Adapta ONE - Editor Literário IA v1.0

# Framework da Interface
streamlit>=1.25.0

# Processamento de Documentos
python-docx>=0.8.11

# Configuração
pyyaml>=6.0  # Para arquivos de configuração YAML

# IA e Processamento de Linguagem Natural (PLN)
openai>=1.0.0
language-tool-python>=2.7.0  # Para gramática e ortografia

# Nota: FastFormat está incluído como módulo local (fastformat.py)
# não requer instalação via pip
